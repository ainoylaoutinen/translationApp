const ProfileHeader = ({ username }) => {
    return (
        <header>
            <h4>
                Hello, welcome {username}
            </h4>
        </header>
    )
}

export default ProfileHeader;